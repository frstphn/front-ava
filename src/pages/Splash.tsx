import { Link } from 'react-router-dom'

// Stub — animation Typed.js + skip sessionStorage à implémenter (brief §4 "Page splash")
export default function Splash() {
  return (
    <main className="splash">
      <p>Splash — à implémenter (Typed.js + sessionStorage)</p>
      <Link to="/accueil">Entrer</Link>
    </main>
  )
}
